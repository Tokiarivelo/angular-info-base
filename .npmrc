# NPM Configuration for Vercel deployment
# This configuration optimizes npm install for production builds

# Disable progress bars to reduce log noise
progress=false

# Skip audit to save time and space
audit=false

# Skip funding messages
fund=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
